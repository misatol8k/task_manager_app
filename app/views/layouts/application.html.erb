<!DOCTYPE html>
<html>
  <head>
     <title>Task Manager</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <header class="blog-header py-3">
      <div class="container nav-scroller py-1 mb-2">
        <ul class="nav d-flex nav-pills">
          <li role="presentation"><a class="p-2 text-muted" href="#">Top</a></li>
          <li role="presentation"><%= link_to t('view.index'), tasks_path, class: "p-2 text-muted" %></li>
          <li role="presentation"><%= link_to t('view.new'), new_task_path, class: "p-2 text-muted" %></li>
          <% if logged_in? %>
            <li role="presentation"><%= link_to 'Profile', user_path(current_user.id), class: "p-2 text-muted" %></li>
            <li role="presentation"><%= link_to 'Logout', session_path(current_user.id), method: :delete, class: "p-2 text-muted" %></li>
            <% if current_user.admin? %>
            <li role="presentation"><%= link_to 'ユーザー一覧', admin_users_path, class: "p-2 text-muted" %></li>            
            <% end %>
          <% else %>
            <li role="presentation"><%= link_to 'Sign up', new_user_path, class: "p-2 text-muted" %></li>
            <li role="presentation"><%= link_to 'Login', new_session_path, class: "p-2 text-muted" %></li>
          <% end %>
        </ul>
      </div>


    </header>
    <div class="container">
      <div class="name">
        <%= yield(:title) %>
      </div>
      <p id="notice"><%= notice %></p>
      <%= yield %>
    </div>
    <footer class="footer mt-auto py-3">
      <div class="container">
        <span class="text-muted">footer.</span>
      </div>
    </footer>
  </body>
</html>
